# Configuration files

Copy the following analysis configuration files into this directory:

- `input_C_borealis_STG15_39395.yaml`
- `run_params.yaml`

Before committing, replace private absolute paths with documented relative paths or configurable variables and confirm that the YAML remains valid.
